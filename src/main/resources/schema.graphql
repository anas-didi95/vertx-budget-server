type Expense {
  id: String!
  item: String!
  price: Float!
  createDate(format: String): String!
  updateDate(format: String): String
  version: Int!
}

type Query {
  expense(id: String): Expense
}
